# Walder — the 0.2 handoff note (retired)

**Nothing here is live.** This note was written on 2026-09-10 for the run-up to
0.2.0, and its state, its strip list and its release checklist all predate
0.2.2. The two sections that were still binding — "Binding rules" and "Process
that has worked" — moved to [`../CONTRIBUTING.md`](../CONTRIBUTING.md) when it
was created, which is what this note was waiting for; the rest is in git
history. The live roadmap is
[`CODENOTCH_GAP_ANALYSIS.md`](CODENOTCH_GAP_ANALYSIS.md) §4, what was built is
in [`BUILD_LOG.md`](BUILD_LOG.md), and why is indexed in
[`DECISIONS.md`](DECISIONS.md).

The file is kept, empty of content, because several documents and one skill
still link to it by name; it exists to say where to go instead.
